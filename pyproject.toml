[project]
name = "autopodcast"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "dropbox",
    "feedgen",
    "mutagen",
    "paramiko",
    "python-dotenv",
    "watchdog",
]

[tool.pytest.ini_options]
pythonpath = ["."]

[dependency-groups]
dev = [
    "pytest>=9.1.0",
]
